
## 2.
>>> from umling.mol import *

## 3.
>>> 'a'
'a'
>>> 42
42
>>> -8.27
-8.27

## 4.
>>> ('hi', 2, 'bye', 2)
('hi', 2, 'bye', 2)
>>> ['hi', 2, 'bye', 2]
['hi', 2, 'bye', 2]
>>> len(['hi', 2, 'bye', 2])
4

## 5.
>>> {'hi', 2, 'bye', 2} == {2, 'bye', 'hi'}
True
>>> len({'hi', 2, 'bye', 2})
3
>>> len(set())
0

## 6.
>>> x = 42
>>> x
42
>>> ('x', x, 'bye')
('x', 42, 'bye')

## 7.
>>> x == 42
True
>>> 1 + 1 == 3
False
>>> {'d', 'a', 'd'} == {'a', 'd'}
True

## 8.
>>> from umling.mol.symbols import a, b, c, d
>>> a
a
>>> from umling.mol.symbols import c, l, o, orange, an, cat
>>> alphabet('cool') == {c, l, o}
True
>>> vocab('an orange cat') == {orange, an, cat}
True

## 9.
>>> a * b
a∙b
>>> epsilon
ε
>>> len(a * b)
2
>>> len(epsilon)
0
>>> letters('cool')
c∙o∙o∙l
>>> words('an orange cat')
an∙orange∙cat

## 9. extra
>>> try:
...     (a,b) * (c,d)
... except Exception as e:
...     print(e)
...
can't multiply sequence by non-int of type 'tuple'

## 10.
>>> x = c * c
>>> a * x * b
a∙c∙c∙b

## 11.
>>> a * b * epsilon
a∙b∙ε
>>> a * b * epsilon == a * b
True
>>> a * b == b * a
False
>>>
string(a * b * epsilon)
(a, b)

## 12.
>>> a * b + a * c
a∙b + a∙c
>>> empty
∅
>>> A = (a + b) * c
>>> A + empty == A
True
>>> A * empty == empty
True

>>> set(a * b + a * c) == {(a, c), (a, b)}
True
>>> set(empty)
set()

## 13.
>>> try:
...     (a, b) * (c, d)
... except Exception as e:
...     print(e.__class__.__name__, e)
...
TypeError can't multiply sequence by non-int of type 'tuple'
>>> a * b * (c, d)
a∙b∙c∙d
>>> {a, b} * c
(a + b)∙c
>>> try:
...     {a, b} * (c, d)
... except Exception as e:
...     print(e.__class__.__name__, e)
...
TypeError can't multiply sequence by non-int of type 'set'
>>> L(a, b) * (c, d)
a∙b∙c∙d
>>> L({a, b}) * (c, d)
(a + b)∙c∙d

## 14.
>>> star(a + b)
(a + b)*
>>> repeat(a + b)
(a + b)∙(a + b)*

## 15.
>>> enum((a + b) * c)
[0] (a, c)
[1] (b, c)
>>> enum(epsilon)
[0] ()
>>> enum(empty)
<empty enumeration>
>>> enum(star(a), 3)
[0] ()
[1] (a,)
[2] (a, a)
...

## 16.
>>> set((a + b + c) - (c + d)) == {(a,), (b,)}
True
>>> a in a + b + c
True
>>> a * c in (a + b) * c
True

## 17.
>>> U = a + b
>>> L = U * c
>>> L
(a + b)∙c
>>> from umling.mol.symbols import dog, cat, saw, chased, the, that
>>> N = dog + cat
>>> V = saw + chased
>>> try:
...     NP = the * RC
...
... except Exception as e:
...     print(e.__class__.__name__, e)
...
NameError name 'RC' is not defined
