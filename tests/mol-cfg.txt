
## Value

>>> from umling.mol import *
>>> from umling.mol.symbols import N, V, sg, pl, isg
>>> not3s = pl + isg
>>> not3s
isg+pl
>>> type(pl)
<class 'umling.mol.lang.Symbol'>
>>> type(not3s)
<class 'umling.mol.lang.Value'>
>>> sheep_value = sg + pl
>>> sheep_value
pl+sg
>>> v = not3s & sheep_value
>>> type(v)
<class 'umling.mol.lang.Value'>
>>> v.atoms
(pl,)
>>> v
pl

## Category

>>> dog = N[sg]
>>> type(dog)
<class 'umling.mol.lang.Category'>
>>> type(dog[0])
<class 'umling.mol.lang.Symbol'>
>>> dog
N[sg]
>>> dog[1]
sg

>>> am_pos = V[isg]
>>> are_pos = V[pl]
>>> is_pos = V[sg]
>>> run_pos = V[not3s]
>>> runs_pos = V[sg]

>>> run_pos
V[isg+pl]
>>> are_pos
V[pl]

>>> bool(not3s & (sg + pl))
True

## U

>>> U
U
>>> U(sg)
True
>>> sg & U
sg
>>> sg & U == sg
True
>>> enum(sg & U)
[0] (sg,)

## empty

>>> empty
∅
>>> empty & sg
∅
>>> enum(sg & empty)
<empty enumeration>
>>> empty & sg == empty
True

## Sheep run

>>> sheep_pos = N[sg+pl]

## Rule and dotted rules

>>> from umling.mol.symbols import S
>>> from umling.mol.variables import form
>>> type(form)
<class 'umling.mol.lang.Variable'>
>>> form
form
>>> r = grule(S[form], N[form], V[form])
>>> r
S[form] -> N[form] V[form]
>>> e = r * sheep_pos
>>> e
S[form] -> N[form] * V[form]
>>> e.bindings
{form: pl+sg}
>>> e = e * run_pos
>>> e
S[form] -> N[form] V[form] * 
>>> e.is_complete()
True
>>> e.cat()
S[pl]

## Chart nodes

>>> from umling.mol.parser import parser
>>> parser
>>> n_node = Node(sheep_pos, 0, 1)
>>> n_node

